---
# tasks file for roles/database

- name: Remove container db if it already exists
  docker_container:
    name: postgresdb
    state: absent

- name: Run DB
  docker_container:
    name: postgresdb
    image: eriau/devops_tp01_db:1.3
    networks:
      - name: network_tp03
    volumes:
      - /data